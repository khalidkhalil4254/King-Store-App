<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.assignment2">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Assignment2">
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".signIn"></activity>
        <activity android:name=".signUp"></activity>
        <activity android:name=".storeHome"></activity>
        <activity android:name=".menu"></activity>
        <activity android:name=".products"></activity>
        <activity android:name=".productPage"></activity>
        <activity android:name=".cartPage"></activity>
        <activity android:name=".checkoutPage"></activity>
    </application>

</manifest>